/**
 * Deprecated — use OptionDetailSheet directly. Kept temporarily so any
 * stale imports compile during the rename. Safe to delete once nothing
 * imports `SignDetailSheet`.
 */
export { default } from './OptionDetailSheet';
