// Default Expo Metro config. Customise as needed (e.g. SVG transformer,
// monorepo support). Kept minimal to mirror Expo's recommended setup.
const { getDefaultConfig } = require('expo/metro-config');

module.exports = getDefaultConfig(__dirname);
