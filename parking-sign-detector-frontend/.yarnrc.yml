# Yarn 4 configuration.
#
# Expo / React Native / Metro do not support Yarn Plug'n'Play. Force the
# classic node_modules linker so Metro can resolve packages the way it
# expects.
nodeLinker: node-modules

# Cache zips next to the project (already covered by .gitignore).
enableGlobalCache: false
